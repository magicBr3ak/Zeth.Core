namespace System.ObjectModel
{
    public enum PopupState
    {
        ON_CLOSE = 1,
        ON_HIDE = 2,
        ON_REPLACE = 3,
        ON_SHOW = 4,
        AFTER_CLOSE = 4,
        AFTER_HIDE = 5,
        AFTER_SHOW = 6
    }
}
